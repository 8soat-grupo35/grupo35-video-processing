apiVersion: v1
kind: Service
metadata:
  name: video-processing-app
spec:
  selector:
    app: video-processing-app
  ports:
    - protocol: TCP
      port: 8000
      nodePort: 32100
      targetPort: 8000
  type: NodePort
  